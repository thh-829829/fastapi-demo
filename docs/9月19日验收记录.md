# 9 月 19 日企业级 MVP 验收记录

验收日期：2026-09-21

## 一、验收范围

本次验收对应 `9月14日起执行计划.md` 中 9 月 19 日的企业级阶段收尾任务，覆盖双角色全流程、异常场景、文档更新和依赖清单。

## 二、自动化测试结果

执行命令：

```powershell
.\venv\Scripts\python.exe -m pytest
```

执行结果：

```text
25 passed in 21.40s
```

测试拆分：

| 测试文件 | 用例数 | 覆盖内容 |
| --- | ---: | --- |
| `tests/test_permissions.py` | 16 | 401、403、200、管理员脱敏、跨用户目标/任务/文档隔离 |
| `tests/test_main_flows.py` | 5 | 注册登录、目标任务、上传删除、RAG、Agent |
| `tests/test_failure_handling.py` | 4 | 空参数、日志失败、RAG 日志降级、上传失败回滚 |

## 三、双角色回归

| 场景 | 结果 |
| --- | --- |
| 无 Token 访问受保护接口 | 401 |
| 无效 Token 访问受保护接口 | 401 |
| 普通用户访问管理员接口 | 403 |
| 管理员访问用户列表、文档列表和日志列表 | 200 |
| 管理员用户列表邮箱脱敏 | 通过 |
| 管理员用户列表不返回密码 | 通过 |
| 管理员文档列表不返回正文 | 通过 |
| 用户 A 查询、修改、删除用户 B 的目标 | 404 |
| 用户 B 在用户 A 的目标下创建任务 | 404 |
| 用户 B 删除用户 A 的文档 | 404 |

## 四、主链路回归

| 链路 | 结果 |
| --- | --- |
| 注册、登录、查询当前用户 | 通过 |
| 创建目标、创建任务、更新完成状态 | 通过 |
| 上传文档、写入用户隔离元数据、删除文档 | 通过 |
| RAG 检索、回答生成、来源返回、日志写入 | 通过 |
| Agent 对话和 JWT 用户身份覆盖 | 通过 |

## 五、异常与降级

| 场景 | 结果 |
| --- | --- |
| 空问题参数 | 422 |
| 日志数据库不可用 | 日志写入返回 `False`，不抛出异常 |
| RAG 问答期间日志写入失败 | 回答正常返回 |
| 向量库写入失败 | MySQL 文档记录回滚，无孤立数据 |

## 六、工程收口

- `README.md` 已补齐启动、配置、权限、日志、测试和接口说明。
- `.env.example` 已创建，且不包含真实密钥。
- `.env.example` 已从 `.gitignore` 中移除忽略规则。
- `requirements.txt` 已补齐运行与测试依赖，并在当前环境通过依赖检查。
- `pytest.ini` 已限制正式测试只收集 `tests/`，避免执行根目录历史原型脚本。
- `admin_service.py` 已补齐到工作区，提交时需纳入版本控制。

## 七、遗留到 9 月 20 日的事项

1. 将 JWT 密钥、MySQL 连接串、Redis 地址和模型名称迁移到统一配置。
2. 合并 `app/core/security.py` 与 `app/core/deps.py` 中重复的鉴权实现。
3. 清理请求体中冗余的 `user_id` 字段。
4. 为企业级 MVP 打里程碑 tag。
