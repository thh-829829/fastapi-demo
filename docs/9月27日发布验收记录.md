# 9 月 27 日 v1.0.0 发布验收记录

计划节点：2026-09-27

实际完成：2026-09-25

## 一、验收范围

1. 完善 README 的项目背景、架构、技术栈、功能、权限、日志、运行和部署说明。
2. 添加桌面端、移动端、API 文档、管理员日志截图和架构图。
3. 清理临时脚本、测试数据、IDE 元数据和敏感配置。
4. 执行最终回归并创建 `v1.0.0` tag。
5. 整理可供简历使用的量化指标。

## 二、文档产物

| 文件 | 用途 |
| --- | --- |
| `README.md` | 项目首页、部署与验收总入口 |
| `docs/演示说明.md` | 5 至 8 分钟面试演示脚本 |
| `docs/简历量化数据.md` | 简历描述和 STAR 表述 |
| `docs/images/architecture.svg` | README 使用的架构图 |
| `docs/architecture.drawio` | 可编辑架构图源文件 |
| `deploy/nginx-ip.conf.example` | 不使用域名时的 HTTP 反向代理配置 |
| `deploy/deploy.sh` | 服务器拉取和重建脚本 |

## 三、截图

- `docs/images/home-desktop.png`
- `docs/images/home-mobile.png`
- `docs/images/api-docs.png`
- `docs/images/admin-logs.png`

## 四、最终回归

执行命令：

```powershell
.\venv\Scripts\python.exe -m pytest -o addopts='' -q
```

验收结果：

```text
42 passed
```

容器状态：

- FastAPI：运行正常
- MySQL：`healthy`
- Redis：`healthy`
- ChromaDB：`healthy`

## 五、清理项

- `.idea/` 从 Git 跟踪中移除并保持忽略。
- 删除旧的 `static/test.html`。
- 删除过时目录清单 `目录.txt`。
- 删除临时截图脚本。
- 根目录测试文档保持被 `.gitignore` 忽略。
- `.env` 保持本地保存且不进入 Git。

## 六、发布结论

- `v1.0.0` 代码、文档、截图和部署产物齐全。
- 面试演示使用公网 IP + HTTP，不把域名和 HTTPS 作为硬性条件。
- 生产化改造方向已在 README 中明确。
