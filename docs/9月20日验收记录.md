# 9 月 20 日部署准备验收记录

验收日期：2026-09-22

## 一、验收范围

本次验收对应《02新小米su7》中 9 月 20 日的部署准备任务：

1. 清理硬编码 JWT、数据库密码、API Key 和模型名称。
2. 补齐 `.env.example`。
3. 编写登录、上传、问答、Agent 冒烟测试。
4. 更新架构说明并确认企业级 MVP tag。
5. 补齐企业级阶段遗留问题。

## 二、配置治理

新增 `app/core/config.py`，统一管理：

- 应用名称、版本、主机和端口。
- MySQL 主机、端口、用户名、密码和数据库名。
- Redis 主机、端口和数据库编号。
- JWT 密钥、算法和有效期。
- DeepSeek API Key、地址、模型、超时和重试次数。
- 硅基流动 API Key、地址和 Embedding 模型。
- ChromaDB 路径和集合名。
- 上传目录和最大文件大小。

`app/db/database.py`、`app/core/security.py`、`app/utils/redis_client.py`、`app/utils/llm_client.py`、`app/utils/vector_store.py`、`app/api/v1/file.py` 和 `app/services/document_parser.py` 已统一接入配置。

同时完成企业级遗留收口：

- `app/core/deps.py` 只转发 `security.py` 的鉴权依赖，不再保留重复实现。
- `app/main.py` 只挂载 `app/api/v1/__init__.py` 的统一路由聚合器。
- `data/`、`app/data/`、`uploads/` 和本地测试文档已加入 `.gitignore`。
- Agent 会话元数据 TTL 和消息数更新时机已修复。

## 三、自动化测试

执行命令：

```powershell
.\venv\Scripts\python.exe -m pytest
```

执行结果：

```text
37 passed in 29.86s
```

| 测试文件 | 用例数 | 覆盖内容 |
| --- | ---: | --- |
| `tests/test_permissions.py` | 16 | 401、403、200、管理员脱敏、跨用户隔离 |
| `tests/test_main_flows.py` | 6 | 主链路、RAG 用户过滤、Agent 身份 |
| `tests/test_failure_handling.py` | 4 | 空参数、日志失败、上传回滚 |
| `tests/test_agent_tools.py` | 4 | Agent 目标/任务工具、RAG 工具和隔离 |
| `tests/test_smoke.py` | 4 | 登录、上传、问答、Agent 四条冒烟链路 |
| `tests/test_config.py` | 3 | 配置来源和环境变量模板 |

## 四、环境与启动验证

- `.env.example` 已包含运行所需的全部配置项。
- 源码中不再包含真实数据库密码、JWT 密钥和 API Key。
- 环境变量加载验证通过。
- Uvicorn 启动验证通过。
- `/docs` 返回 200。
- 静态前端页面返回 200。
- Alembic 位于 `a632c3abf6a8 (head)`。
- 依赖检查无冲突。

## 五、里程碑

- `enterprise-mvp` tag 已存在。
- `rag-mvp` tag 已在本地 RAG 完成提交上补建。
- 新功能提交和 tag 仍需推送到 GitHub。

## 六、遗留到 9 月 21 日

1. 编写 Dockerfile。
2. 验证容器读取环境变量。
3. 连接容器与 MySQL、Redis。
4. 构建并运行 FastAPI 镜像。
