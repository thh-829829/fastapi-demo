\# RAG 效果调优报告



\## 一、调优背景



本次调优针对项目核心知识库问答能力，验证文档解析、向量召回、Prompt 生成全链路效果，定位并修复链路问题，输出生产环境最优参数组合。



\## 二、测试环境



\- 测试文档：《AI Agent 项目执行计划表》（小米 su7.docx）

\- 文档规模：全文约 10000 字，包含正文段落与多组表格

\- 嵌入模型：BGE 系列中文向量模型

\- 基础分块策略：固定长度分块，chunk\_size=500，chunk\_overlap=100

\- 测试维度：细节事实类、总结归纳类、跨章节对比类三类典型场景



\## 三、问题与修复记录



表格



| 问题描述 | 根因分析 | 修复方案 | 修复效果 |

| --- | --- | --- | --- |

| docx 文档分块仅 2 块，表格内容完全丢失 | python-docx 默认 paragraphs 不读取表格内容 | 新增表格遍历逻辑，逐行提取单元格文本合并入全文 | 分块数量提升至 22 块，表格内容完整入库 |

| 向量检索报错维度不匹配 | 入库与查询使用两套不同的 Embedding 模型 | 统一入库、查询全链路使用同一套向量模型，严格遵循同源原则 | 维度错误修复，检索链路正常 |

| 检索永远只返回 1 条结果 | vector\_store 中 search\_similar 函数 return 缩进错误，写在 for 循环内部 | 调整 return 缩进至循环外，完整返回 top\_n 条结果 | 召回数量正常，可自由配置 top\_n 参数 |

| 总结类问题回答 “未找到相关信息” | 章节标题与具体内容被分块切割，召回仅命中标题分块，未命中内容分块 | 扩大召回数量至 top\_n=8，兜底内容分块召回 | 三类问题回答准确率提升至 100% |



\## 四、参数对比测试



\### 检索数量对比



表格



| 召回数量 | 召回覆盖率 | 回答准确率 | 适用场景 |

| --- | --- | --- | --- |

| top\_n=3 | 33% | 33% | 短文档、单段落场景，性能最优 |

| top\_n=5 | 67% | 67% | 中等篇幅文档，平衡性能与效果 |

| top\_n=8 | 100% | 100% | 长文档、多章节表格场景，效果最优 |



\## 五、生产环境最优参数



\- 分块参数：`chunk\_size = 500`，`chunk\_overlap = 100`

\- 检索参数：`top\_n = 8`

\- 结论：该参数组合下，三类测试场景全部回答准确，回答可追溯来源，兼顾召回覆盖率与回答质量。



\## 六、后续优化方向



1\. \*\*分块语义优化\*\*：为每个分块注入所属章节标题，提升单块语义完整性，可将 top\_n 回落至 5，降低性能开销

2\. \*\*嵌入模型升级\*\*：更换为 bge-large-zh-v1.5 大尺寸中文模型，提升语义区分度与排名准确率

3\. \*\*精排阶段优化\*\*：新增 Rerank 二次排序，在宽召回基础上做精筛，兼顾召回率与精准度

